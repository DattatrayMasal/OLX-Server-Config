spring:
 
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/advertiesdb
    username: root
    password: root
  jpa:
    hibernate.ddl-auto: update
    properties:
     hibernate:
      dilect: org.hibernate.dialect.MySQLDialect
